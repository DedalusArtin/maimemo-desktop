# 墨墨背单词桌面版 — 项目交接文档

> 用途:右下角常驻的墨墨风格单词学习悬浮窗 + 墨墨开放 API Web 端,与「墨墨背单词」App 双向联动。

---

## 1. 项目路径与文件结构

**主目录:`D:\墨墨背单词\`**(git 仓库,main 分支,已 push 到公开仓库 https://github.com/DedalusArtin/maimemo-desktop)

```
D:\墨墨背单词\
├── app.py                 # 桌面端后端(pywebview 窗口 + 墨墨API + 有道词典补全 + 本地间隔记忆)
├── web\
│   └── index.html         # 桌面悬浮窗 UI(紧凑卡片 + 展开学习面板,单文件内联 CSS/JS)
├── web版\                 # 浏览器 Web 端(仿官方 openapi-demo)
│   ├── server.py          #   本地服务器:端口 8790,代理 /api/v1/* 到墨墨开放平台
│   ├── index.html         #   Web 端页面(7 大分组,数据驱动渲染)
│   └── 启动Web版.bat      #   一键启动(自动开浏览器)
├── installer\
│   └── installer.iss      # Inno Setup 6 安装脚本(ISCC.exe installer\installer.iss 出包)
├── 墨墨背单词.exe          # PyInstaller 打包产物(绿色版,双击即用)
├── config.json            # 配置 + 墨墨 token【已 gitignore,含真实 token】
├── config.example.json    # 配置模板(无 token,用于 git)
├── words.txt              # 本地词表(每行 "单词 | 释义")
├── mastered.txt           # 已掌握单词(运行期生成,删行可恢复)
├── cache.json             # 有道词典缓存(运行期生成)
├── review.json            # 模糊词的间隔记忆状态(运行期生成)
├── README.md / 说明.txt   # 说明文档
└── .gitignore
```

**安装包:** `安装包\墨墨背单词-安装程序-v1.1.0.exe`(Inno Setup 6 制作,~15MB,带卸载程序:设置→应用→卸载,或安装目录 unins000.exe)。安装包不含 config.json(token 隐私,首次启动自动生成默认配置)。

**其他相关路径:**
- 环境 Python:`G:\miniconda\envs\dedalus_hermes\python.exe`(Py3.11,已装 requests/edge-tts/pywebview/pyinstaller/cffi)
- SSH 密钥:`~\.ssh\id_ed25519`(公钥已添加到 GitHub)
- 代理:Clash Verge `127.0.0.1:7890`(git 全局 http.proxy 是旧错端口 7897,推送需 `git -c http.proxy=http://127.0.0.1:7890`)

---

## 2. 运行方式

### 开发模式
```bash
# 依赖
G:\miniconda\envs\dedalus_hermes\python.exe -m pip install requests edge-tts pywebview pyinstaller cffi

# 桌面悬浮窗(注意:从 Hermes 终端启动需先 unset PYTHONPATH)
cd /d/墨墨背单词 && python app.py

# Web 端
cd /d/墨墨背单词/web版 && python server.py   # http://127.0.0.1:8790
```

### 生产模式
双击 `D:\墨墨背单词\墨墨背单词.exe`,或桌面快捷方式「墨墨背单词」。
Web 端双击「墨墨背单词Web端」快捷方式。

---

## 3. 功能清单(已实现)

### 桌面悬浮窗
- 常驻屏幕四角(默认右下角),始终置顶,无边框
- 紧凑模式:自动轮播 单词 → 中文释义+例句 → 下一个
  - 左键 = 显示释义/下一个;右键 = 展开;🔊 发音;◀ 回看;⏱ 自动/手动开关
- 展开学习面板(470×730):词义列表 + 例句高亮 + 音标 + 发音
  - ✓ 认识 / ~ 模糊 / ✗ 忘记 三键复习
  - 添加单词:查找 → 预览词义/音标/例句 → 确认添加
  - 📥 导入云词本(从墨墨 App 云词本批量导入)
  - ⚙ 设置:token、轮播秒数、角落、发音方式、自动轮播开关
- 发音:edge-tts(需联网),失败自动切 Windows SAPI

### 与墨墨背单词联动(开放 API)
- 拉取「今日学习单词」到词池
- 添加单词双向同步到墨墨账号
- 词义/例句来自账号数据,缺省用有道词典补全(带缓存)

### 本地间隔记忆
- 认识 → mastered.txt(移出)
- 模糊 → review.json,10 分钟后自动回来
- 忘记 → 移到队尾马上再看

### Web 端(浏览器)
- 仿官方演示,7 大分组:学习数据/单词/释义/例句/助记/云词本/记忆卡
- token 与桌面版共用 config.json;内置 API 代理避免 CORS

---

## 4. 墨墨开放 API 要点

- **token 获取**:墨墨背单词 App → 我的 → 更多设置 → 实验功能 → 开放 API(非开发者注册)
- **base**:`https://open.maimemo.com/open/api/v1/memo`
- **认证**:`Authorization: Bearer <token>`
- **关键端点**:
  - `POST /study/get_today_items` {is_finished, limit≤1000} → 今日学习单词
  - `POST /study/add_words` {words:[{id}], advance} → 添加单词
  - `POST /study/get_study_progress` / `query_study_records` / `advance_study`
  - `GET /vocabulary?spelling=` / `POST /vocabulary/query` → 查单词 ID
  - `GET /interpretations?voc_id=` → 释义;`GET /phrases?voc_id=` → 例句;`GET /notes?voc_id=` → 助记
  - `GET /notepads` / `GET /notepads/{id}` → 云词本(批量导入用)
- **频控**:20 次/10 秒、40 次/60 秒、2000 次/5 小时(背单词)、8000 次/5 小时(记忆卡)
- **限制**:公测接口;需 App 当日打开过初始化今日词表;需开启"自动同步"
- **文档**:https://open.maimemo.com/#/ ;OpenAPI 规范 https://open.maimemo.com/api_bundle.yaml

---

## 5. 当前问题清单(待修)

1. **同步失败 ✅ 已修复(v1.1)**
   - 根因:app.py 漏 `import random`,词池初始化最后一步 shuffle 抛 NameError,被兜底捕获后整场误报「同步失败·本地模式」(此前误判为超时/无重试)。
   - v1.1 改动:补 import;网络抖动自动重试一次;同步失败提示翻译成人话(401=token 过期、超时=网络、429=限流);设置面板新增「🔄 立即同步墨墨词表」无需重启;鼠标悬停状态标签可看具体原因。

2. **词表为空(用户操作导致)**
   - 现象:测试时把词全点了「认识」→ mastered.txt 全占 → 本地词清空。
   - 建议:①「认识」语义改为墨墨式(不删除,只推迟复习);② 列表空时提示"可从 mastered.txt 恢复 / 导入云词本"。

3. **窗口圆角不完美**
   - 现象:冻结版 pywebview 透明窗口未真正透明,当前用 `body{background:#14161c}` 掩盖方角(视觉近似圆角)。
   - 建议:排查 pywebview 6 + pythonnet 在 PyInstaller 冻结环境的透明窗口;或改用 Tauri/原生 WebView2。

4. **打包坑(pywebview 6 冻结)**
   - 必须:`--collect-all webview --collect-all cffi --hidden-import clr_loader.ffi.cffi`
   - cffi 必须装进环境(否则 clr_loader 在冻结态 import 失败,报 `No module named 'cffi'`)
   - pywebview 6 只支持 `window.expose(函数)`(不支持暴露类实例)

5. **安装包 ✅ 已标准化(v1.1.0)**
   - Inno Setup 6 已装(winget,用户级 `%LOCALAPPDATA%\Programs\Inno Setup 6`);中文语言包不随包附带,需从 jrsoftware/issrc 仓库下载 `Files/Languages/ChineseSimplified.isl` 放入其 `Languages\` 目录。
   - `installer/installer.iss` 已脱敏(AppPublisher=开源社区)且不含 config.json;`ISCC.exe installer\installer.iss` 出包到 `安装包\`。

6. **GitHub ✅ 已完成**
   - 公开仓库:https://github.com/DedalusArtin/maimemo-desktop,提交用 noreply 邮箱;敏感文件(config.json/cache.json/mastered.txt/review.json/*.exe/安装包)全部 gitignore;README 标注 AI 生成。

7. **未实现/可扩展**
   - 墨墨 App 里整本词书无法导出(API 只开放"今日学习"和"云词本"),批量导入需走云词本。
   - 发音 edge-tts 依赖联网;例句/释义离线依赖 cache.json 缓存。
   - 记忆卡(Markji)接口已在 Web 端侧栏留位,未接入桌面端。

---

## 6. 用户原始需求汇总(按时间)

1. 右下角不断显示要背的单词,用于巩固
2. 用墨墨开放 API 联动(https://open.maimemo.com/#/ 提供)
3. 界面像墨墨、要显示例句、发音
4. 打包成软件安装到 D 盘(D 盘 = 用户指定项目路径)
5. 自动/手动开关、不展开也能发音、可回看上一个
6. 左键出中文+例句、右键展开学习
7. 认识/模糊/忘记 选项 + 复习时间优化
8. 添加单词带查找功能
9. 成品做成安装包
10. 源码上传 GitHub(用用户账户)
11. 恢复丢失的 git 环境 + 添加 SSH(解决 GitHub 文件下载失败)
12. 登录 Chrome 恢复(注:需用户本人操作,涉及账号密码/两步验证,无法代做)
13. 修复同步失败 + Inno Setup 打包 + 上传 GitHub 公开仓库并标注 AI 生成(脱敏)

---

## 7. 打包命令(重出 exe)

```bash
cd /d/墨墨背单词 && unset PYTHONPATH && \
G:\miniconda\envs\dedalus_hermes\python.exe -m PyInstaller --noconfirm --clean --windowed \
  --name 墨墨背单词 --collect-all webview --collect-all cffi \
  --hidden-import clr_loader.ffi.cffi --add-data "web;web" app.py
# 产物在 dist/墨墨背单词/,拷回根目录即可
```
